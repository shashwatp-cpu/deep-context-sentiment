import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { VisualEditsMessenger } from "orchids-visual-edits";
import ErrorReporter from "@/components/ErrorReporter";
import Script from "next/script";
import { ClerkProvider } from "@clerk/nextjs";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "EliminateContext | Best AI Social Listening & Sentiment Analysis Tool (2025)",
  description: "The world's first context-aware AI sentiment analysis platform. Detect sarcasm, predict crises, and understand deep context behind every mention. The AI alternative to Brandwatch and Talkwalker.",
  keywords: ["AI sentiment analysis", "social listening", "context-aware NLP", "brand monitoring", "crisis prediction", "voice of customer"],
  openGraph: {
    title: "EliminateContext | AI-Powered Social Listening",
    description: "Stop counting keywords. Start understanding intent. The first Generative AI sentiment engine that detects sarcasm, predicts crises, and understands deep context.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <ClerkProvider>
      <html lang="en">
        <body
          className={`${geistSans.variable} ${geistMono.variable} antialiased`}
        >
          <Script
            id="orchids-browser-logs"
            src="https://slelguoygbfzlpylpxfs.supabase.co/storage/v1/object/public/scripts/orchids-browser-logs.js"
            strategy="afterInteractive"
            data-orchids-project-id="a235f3b5-a4fe-4092-a0af-e3ba60019a88"
          />
          <ErrorReporter />
          <Script
            src="https://slelguoygbfzlpylpxfs.supabase.co/storage/v1/object/public/scripts//route-messenger.js"
            strategy="afterInteractive"
            data-target-origin="*"
            data-message-type="ROUTE_CHANGE"
            data-include-search-params="true"
            data-only-in-iframe="true"
            data-debug="true"
            data-custom-data='{"appName": "YourApp", "version": "1.0.0", "greeting": "hi"}'
          />
          {children}
          <VisualEditsMessenger />
        </body>
      </html>
    </ClerkProvider>
  );
}

